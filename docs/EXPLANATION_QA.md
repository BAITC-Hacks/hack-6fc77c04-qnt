# Качество объяснений — 23.09.2026

Точечное продолжение `7c6c995`, без миграции стека, изменения API и исходного CSV. Fetch перед работой: новых опубликованных коммитов в main и feat/contractor-ui не было. Несохранённая работа в локальной копии Бекзат недоступна. Её промпт, UI_QA, исходные скриншоты и основной style.css не менялись.

## Пять изменений и приёмка

| Изменение | Критерий | Результат |
|---|---|---|
| Промпт и релевантность источников | Специализация под формат выше общих похвал; только исходные цитаты | Явные приоритеты и примеры в промпте; отдельный отбор фрагментов для запроса; регрессии и реальные AI-пробы |
| Краткость | Убрать повтор имени, цены, даты, языка и часов из абзаца, сохранить проверяемость | На 13 карточках средняя длина 277,2 → 216,5 знака (−21,9%); факты остаются в evidence |
| Роль AI | Отметка до объяснения, цитата отдельным блоком; fallback не назван AI | «AI выбрал факт» / «Без AI · по данным каталога»; тест порядка и экранирования текста |
| Читаемый результат | Русские подписи, суммы/часы/даты, без дублей, отдельные счётчики и оговорка | Новый компонент ResultDetails; тесты и браузер на реальном API |
| Публичная документация | Демо и воспроизводимый запуск сохранены; частные операции вне Git | Инструмент и инструкции скопированы и проверены вне репозитория, ROOT исправлен; история не переписана |

В main.tsx изменены только импорт и места вывода сводки/карточек. Компоненты и стили находятся в отдельных ResultDetails.tsx/result-details.css: будущий дизайн можно соединить с ними без переноса формы, транспорта и логики отмены запросов.

## Содержательная проверка

Сравнение с `7c6c995`: Алматы, 10.10.2026, бюджет 10 млн, без языка/часов. Ведущий/корпоратив, Фотограф/свадьба, Ресторан/свадьба, Флорист/свадьба, Банкетный зал/свадьба — всего 13 показанных карточек, включая повторяющиеся профили площадок. Метрика длины относится только к этой выборке, не ко всему каталогу. ID и порядок совпадают с базой.

- Куррапика: специализация на корпоративах и деловых встречах, а не харизма.
- Аня: развлечения и танцы; источник не доказывает особый корпоративный опыт, такой вывод не добавляется.
- Сон Гоку: европейская подача, юмор и традиции — атрибутированная цитата, не гарантия результата.
- Свадебные фотографы: репортажный подход и опыт. У HK-30583 мало содержательных сведений о свадьбах; «эстетика, атмосфера, детали» остаётся слабым основанием. Это ограничение каталога, не доказанная уникальная компетенция.
- Лайв-бэнд: состав и репертуар; не утверждаем, что оборудование включено в цену.
- Рестораны: кухня, панорама, интерьер; число гостей/рассадка/включённые услуги неизвестны.
- Флорист: авторское цветочное оформление; не придумываем стиль и состав заказа.

Первый реальный AI-прогон фотографов выбрал нерелевантный список концертных клиентов, несмотря на правило в промпте. Поэтому фрагмент с «помимо/кроме» и упоминанием запрошенного формата теперь исключается из списка для обоих селекторов, если есть альтернатива. Повторный реальный запрос исправление подтвердил. Лексическое правило ограничено и не заменяет семантическую оценку; при отсутствии альтернатив исходная цитата сохраняется.

Промпт уточнён по [официальным рекомендациям GPT-4.1](https://developers.openai.com/api/docs/guides/latest-model?model=gpt-4.1): явные приоритеты и примеры, проверка на конкретных случаях. Модель и Responses API не менялись.

## Проверки

- Backend: **82/82 PASS**, включая ограничения расходов, fallback, контракт, исходные цены/даты, новые регрессии выбора факта и краткости.
- Frontend: **17/17 PASS**, TypeScript и production build PASS. Новые тесты используют серверный рендер React: это не имитация браузерного клика.
- Полный календарь: **30 600 запросов**, 12 452 карточки, 100 дней; 5,980 с, 0 платных вызовов. Исходы и SHA-256 CSV совпадают с базой.
- Браузер: настоящий API, ENABLE_AI=false, без UI-моков. Плотная/редкая/пустая выдача и отсутствие категории, русские факты без повторов, локальная отметка; ширины 320/390/768/1280 без горизонтального переполнения, карточка при 390 визуально просмотрена. Транспорт/отмена запросов не менялись; старые проверки гонки/таймаута не выдаются за новые.
- Отдельная копия текущих исходников без .env: новая установка frontend через `npm ci --offline` из локального npm-кеша, 17 тестов и build PASS; 82 backend-теста с существующей venv PASS. При удалённых переменных ключа/AI: статика HTTP 200, подбор HTTP 200 и три local-карточки через TestClient. Новая Python-venv в этом проходе не создавалась, зависимости не менялись.
- Новый снимок `assets/evidence-results.png`: локальный режим, 1280 px, реальный каталог. Старые снимки Бекзат сохранены.

Реальные AI-пробы (`gpt-4.1-mini-2025-04-14`, HTTP 200):

| Запрос | Секунды | Токены вход / выход |
|---|---:|---:|
| Ведущие | 2,488 | 507 / 48 |
| Флорист | 0,967 | 387 / 20 |
| Фотографы до исправления списка | 1,644 | 554 / 48 |
| Лайв-бэнд | 1,147 | 545 / 20 |
| Рестораны | 1,632 | 621 / 48 |
| Фотографы после исправления списка | 1,583 | 490 / 48 |

Все ответы прошли проверку ID/точных цитат; на первой пробе фотографов провалена содержательная релевантность, затем исправлена. Повторы первых двух из кеша — 0,0024/0,0008 с. Всего 6 успешных обращений и 1 неудачная сетевая попытка в sandbox; счётчик учитывает все 7, не сбрасывался. Это единичные пробы, не p95 и не независимая экспертная оценка.

Эта работа не выполняла публикацию на VPS. Ссылка на демо не означает, что там уже новая версия.
