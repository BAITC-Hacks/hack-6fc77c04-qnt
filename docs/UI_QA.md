# UI QA — Event Editorial

Проверка 23.09.2026. Ветка `feat/contractor-ui`, исходный HEAD `7c6c995`.
Изменены только frontend и этот отчёт. Backend, контракт, README и деплой не изменялись.

## Изменения

Афишная композиция: крупная типографика, тёплый фон, киноварный акцент, карточки-билеты с номерами. На широком экране результат занимает большую часть рабочей области. Системные шрифты, без новых зависимостей, внешних ассетов и анимаций.

Сохранены заголовок «Нужные люди. Под ваше событие.», форма и сценарии API. QNT — команда, Firebird — кейс. Технические названия известных фактов заменены русскими подписями; значения и порядок сервера не меняются. Сохранены цена «от», AI/local и synthetic/city_imputed/price_imputed. Пояснено участие программного кода и AI; учебный календарь не обещает бронирование.

Рядом с кнопкой поиска показан статус. Явная кнопка переводит фокус к результату; завершение запроса не вызывает автоматической прокрутки. Поля имеют размер шрифта 16px, кнопки и раскрывающиеся факты — высоту не менее 44px.

## Фактические проверки

- `npm.cmd test --prefix frontend`: 12/12 PASS. Фикстуры и транспорт: 422/503, не-JSON, сеть, таймаут. Это модульные проверки с подменой fetch.
- `npm.cmd run build --prefix frontend`, `VITE_DEMO_MODE=false`: PASS, TypeScript + Vite 7.3.6. HTML 0.44 kB (gzip 0.33), CSS 7.92 kB (2.33), JS 236.41 kB (74.20). Суммарно примерно 244.77 kB / 76.86 kB gzip. QA-скриншоты не входят в dist.
- Production UI с настоящим текущим FastAPI на `127.0.0.1:8001`, `ENABLE_AI=false`, без ключа. Headless Microsoft Edge, Playwright из временного окружения; пакет не добавлен в зависимости приложения.
- Ширины 320, 375, 390, 768, 1024, 1440 px: реальные три карточки, раскрытие фактов, сохранение порядка имён и всех значений evidence, бюджет 1000 ₸ → no_matches, отсутствие горизонтального переполнения. Это эмуляция размеров, не физические телефоны.
- Реальный API: category_missing («Нет категории»), короткая выдача («Редкие флористы»), local-объяснения; повтор после ошибки возвращает реальную выдачу.
- Клавиатура: Enter на переходе переводит фокус к заголовку результата, Enter раскрывает факты. Связи label/input, размер полей и высота summary проверены автоматически.
- Перехват в браузере: 503 показывает ошибку без карточек и без демоподмены, параметры сохраняются. Отложенный ответ после изменения формы не возвращает старые карточки; следующий реальный запрос корректен. Искусственные две карточки, AI, три флага и очень длинное имя проверены на 320 px. Это проверки отображения, не реальная генерация AI.
- Отдельный Vite на 5174 с `VITE_DEMO_MODE=true`: виден демобаннер, три карточки, запросов к API нет. В production баннер отсутствует и запросы идут к реальному backend.
- Во время сценариев ошибок JavaScript не обнаружено. API-клиент не изменён: таймаут 12 секунд и защита от устаревших ответов сохранены.
- Скриншоты настоящей выдачи: `frontend/qa/screenshots/editorial-1440.png`, `editorial-390.png`; визуально просмотрены.

Повтор браузерной проверки: запустить backend с текущим production dist, задать `PLAYWRIGHT_MODULE_PATH` к установленному вне проекта Playwright, затем `node frontend/qa/editorial.cjs`. При другом адресе задать `QA_ORIGIN`. Скрипт требует Edge и обновляет два QA-скриншота.

## Ограничения проверки

Физический телефон, экранная клавиатура, Safari/iOS, screen reader и взаимодействие с нативным календарным попапом не проверены. Реальная модель AI и её fallback не запускались. Не проводились Lighthouse и замеры на медленной сети; приведён размер сборки, не обещание скорости. Публичный сервер и деплой не проверялись и не менялись. Зелёный CI не заявляется; проверки выполнены локально. Push и merge не выполнялись.

## Интеграция origin/main 83eba1c, 23.09.2026

По поручению Бекзата изменения Естая приняты за функциональную основу. Конфликт main.tsx разрешён использованием ResultSummary и ContractorCard из main. Сохранены разделение объяснения и цитаты, форматирование фактов, счётчики и честная маркировка «Без AI». Оформление этих компонентов адаптировано к Event Editorial; переход с формы к результату сохранён.

- Vitest: 17/17 PASS, включая 5 новых проверок ResultDetails Естая.
- TypeScript + production build: PASS. HTML 0.44 kB / gzip 0.33; CSS 9.65 / 2.67; JS 239.27 / 75.04. Итого 249.36 kB / 78.04 kB gzip.
- Повторён frontend/qa/editorial.cjs против заново запущенного backend текущей версии на 8002, ENABLE_AI=false: все шесть ширин и сценарии PASS. Три реальных исхода, короткая выдача, клавиатура, отсутствие переполнения, повтор после подменённой ошибки и отмена позднего ответа. Подмены AI/флагов/двух карточек остаются только проверкой рендеринга.
- Скриншоты обновлены; desktop визуально просмотрен. Деморежим отдельно повторно после интеграции не запускался (фикстуры покрыты тестами). Ограничения физического телефона, Safari, реального AI и публичного сервера остаются в силе.
- Backend, README и остальные входящие файлы приняты из коммита Естая без дополнительных правок. Это интеграция main в рабочую ветку, не слияние в main и не деплой.

## Дизайн по референсу Бекзата, 23.09.2026

Первый экран полностью перекомпонован: фотографический фон, антиква, светлая форма справа; результаты — широкие горизонтальные карточки ниже. На мобильном — последовательная вертикальная композиция. Компоненты ResultSummary/ContractorCard и API сохранены. Из изображения не перенесены выдуманные рейтинги, «лучшие» подрядчики и ошибочная атрибуция local как AI.

Фон `frontend/public/event-dinner.webp` создан встроенным imagegen, не является фотографией реального подрядчика. Размер 1536×1024, 111914 байт; сжатие WebP quality 78. Внешних запросов за изображениями/шрифтами и новых runtime-зависимостей нет. Pillow установлен только во временное QA-окружение.

Промпт генерации: “Generate a photorealistic website hero background, landscape 1536x1024. Elegant intimate evening event dinner in a high-rise venue, floor-to-ceiling windows and distant city lights at dusk. Warm candlelight, ivory flowers, fine glassware and linen on a dinner table. Main floral arrangement in center-right. Left third very dark and uncluttered for overlay headline; right third can be covered by a UI form. Cinematic natural editorial photography, espresso black shadows, warm champagne highlights, quiet premium mood. No people, NO text, no letters, no signage, no logos, no interface. This is a background asset, not a screenshot.”

Проверки: 17/17 frontend-тестов PASS, production build PASS (VITE_DEMO_MODE=false). Браузерный QA против реального обновлённого backend на 8002 без AI: все сценарии PASS на 320/375/390/768/1024/1440, включая пустую и короткую выдачу, отсутствие категории, клавиатуру, отмену позднего ответа, повтор после искусственной 503. AI/флаги/длинный текст/две карточки проверены подменами ответа. Скриншоты desktop/mobile обновлены и визуально просмотрены. Ограничения физических устройств и реального AI прежние.

Размер production: JS 239.56 kB (gzip 75.09), CSS около 10.3 kB (gzip около 3), HTML 0.44 kB; плюс WebP 111.91 kB. Общий объём около 362 kB, передача с gzip для кода около 191 kB. Lighthouse и реальные сетевые замеры не проводились. Backend, README, контракт, деплой не изменялись.

## Компактные условия и метаданные, 23.09.2026

До поиска сохранён фотографический первый экран с полной формой. После успешного ответа API (включая пустую выдачу и отсутствие категории) форма скрывается, в тёмной шапке показаны город, дата, формат, категория, бюджет, язык и длительность. Результат получает фокус без анимации, страница переходит к началу нового состояния. Это заменяет прежний сценарий ручного перехода после ответа. «Изменить условия» открывает сохранённую форму и фокусирует город. Ошибка API не скрывает форму. Loading, отмена запроса и защита от позднего ответа сохранены.

Добавлены favicon.svg, favicon.ico (16/32/48), apple-touch-icon.png (180×180), оригинальные og-image.svg и og-image.png (1200×630). SVG нарисованы кодом; PNG получены браузерным рендерингом SVG. title/description, lang, charset, viewport, theme-color, canonical, Open Graph и Twitter Card включены в исходный и production HTML. Абсолютные HTTPS URL используют ранее предоставленный Бекзатом домен https://qnt.l33t.kz; название QNT Match не используется.

Проверено:
- 17/17 Vitest, TypeScript и production build PASS. HTML 2.58 kB / gzip 0.79; CSS 11.66 / 3.25; JS 241.24 / 75.40. Фон WebP остаётся 111914 байт; OG и apple-иконка не загружаются как часть интерфейса страницы.
- Реальный локальный backend без AI: 320/375/390/768/1024/1440 px, выдача, пустая и короткая выдача, category_missing, отсутствие горизонтального переполнения. Автотест проверяет скрытие формы, видимость заголовка результата в viewport, фокус, scrollY=0, сохранённый бюджет при возврате, Enter на фактах. Пустой результат тоже сворачивает форму.
- Подменённая 503 оставляет форму открытой, повтор работает; отменённый поздний ответ не заменяет результат. Длинный текст, AI и дополненные данные проверены подменами. Ошибок JS нет.
- Явный деморежим: три карточки, компактная шапка, возврат к форме — PASS.
- Production HTML: title/lang/OG/Twitter проверены в браузере. Все четыре ссылки favicon/ICO/apple/OG возвращают успешный локальный ответ с не-HTML content-type; OG декодируется как 1200×630.
- Скриншоты обновлены: editorial-1440.png и editorial-390.png теперь показывают состояние после поиска; before-search-1440.png — исходный экран. Mobile и OG визуально просмотрены.

После деплоя Естаю: проверить https://qnt.l33t.kz/favicon.svg и /og-image.png, серверный HTML с метаданными, затем реальный предпросмотр ссылки в чатах (возможен старый кеш). Сейчас новые ассеты проверены только локально; публикация, чат-превью и физический телефон не проверены. Backend, API-контракт и деплой не изменялись.
